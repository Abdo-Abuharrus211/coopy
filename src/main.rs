// Copy the notes from target folder containing the correct frontmatter tags.

fn main() {
    let folders = ["Blog", "KnowledgeBase", "Self-Learning"];
    let tags = ["publish"]; // This frontmatter tag to be checked if it's true or false


}
